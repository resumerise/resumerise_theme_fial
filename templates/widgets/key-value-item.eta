<div class="key-value-item <%= it.classes %>" id="<%= it.id %>">
<% if(it.label) { %>
    <label>
        <%= it.label %>
    </label>
<% } %>
<% if(it.value) { %>
    <span>
        <%= it.value %>
    </span>
<% } %>
</div>